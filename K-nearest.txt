Let m <- no_of_training_data_samples(no_of_manholes)
Let p <- unknown_point
for i = 0 to m:
        Calculate Euclidean Distance d(arr[i], p)
Set(S) <- K-Smallest Distances obtained
Return (Majority label among S)
if(orange)
        Area(2km radius around point p) = DANGER
else if(red)
        Area(2km radius around point p) = ALERT
else
        Area(2km radius around point p) = SAFE
